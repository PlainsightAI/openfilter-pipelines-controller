apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openfilter-pipelines-runner.fullname" . }}-test-connection"
  labels:
    {{- include "openfilter-pipelines-runner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openfilter-pipelines-runner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
